import React from "react";
import { ScrollView, StyleSheet } from "react-native";

const RandomExperienceScreen = () => {
    return (
        <ScrollView>

        </ScrollView>
    );
};

const styles = StyleSheet.create({

});

export default RandomExperienceScreen;
